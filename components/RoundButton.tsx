import { Pressable, View } from "react-native";

type Props = {
  title: string;
};
export default function RoundButton({title}: Props) {
  return (
    <View>
      <Pressable>
        
      </Pressable>
    </View>
  );
}
